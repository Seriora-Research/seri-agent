# Commands

Slash commands inside a seri session:

`/mode` `/effort` `/undo` `/restore` `/rewind` `/clear` `/compact` `/memory` `/trajectory` `/usage` `/exit` `/model` `/setup` `/login` `/signup` `/logout` `/config` `/permissions` `/skills` `/mcp` `/hooks` `/max-turns` `/profile` `/plan`
